import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    callContainerBase: {
      position: 'absolute',
      width: '100%',
      height: '100%',
    },
    callContainerLandscape: {
      flexDirection: 'row',
    },
  });