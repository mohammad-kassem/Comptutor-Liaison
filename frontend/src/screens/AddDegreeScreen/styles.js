import { StyleSheet } from "react-native";

export default StyleSheet.create({
  sectionPrompt: {
    fontSize: 20,
    fontWeight: "500",
    marginVertical: 25,
  },

  buttonsContainer: {
    flexDirection: "row",
    justifyContent: "space-between",
    marginTop: "70%",
  },
});
